<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>

        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n"/>
        </Console>

        <!-- Rolling File Appender to log to a unique file each time -->
        <RollingFile name="FileAppender" 
                     fileName="logs/app-%d{yyyy-MM-dd-HH-mm-ss}.log" 
                     filePattern="logs/app-%d{yyyy-MM-dd-HH-mm-ss}.log.gz">
            <PatternLayout>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n</pattern>
            </PatternLayout>
            <Policies>
                <!-- Rolls the file every day and compresses the old logs -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <!-- Roll the file after it exceeds 10MB -->
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
        </RollingFile>

    </Appenders>

    <Loggers>
        <!-- Define logging for your classes -->
        <Logger name="com.yourpackage" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Logger>

        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Root>
    </Loggers>
</Configuration>
